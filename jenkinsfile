pipeline {
    agent any

    triggers {
        githubPush()
    }

    stages {
        stage('Checkout') {
            steps {
                checkout scm
// clone the jenkins file
            }
        }

        stage('Build') {
            steps {
                sh 'echo Build step here'
            }
        }
    }
}
